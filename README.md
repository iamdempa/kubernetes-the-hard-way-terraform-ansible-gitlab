# kubernetes-the-hard-way-terraform-ansible-gitlab

Provisioning a kubernetes cluster inspired by the Kelsey Hightower's `kubernetes-the-hard-way` with `terraform`, `ansible` and a specific `gitlab-runner`